
		<div class="card"><strong><%= comment.name %> Dice:</strong>
		<p><%= comment.body %></p>
		<p><%= time_ago_in_words(comment.created_at) %> Ago</p>
		</div>


		<p>
			<% if can?(:destroy, Comment) %>
				<%= link_to 'Delete', [comment.post, comment], method: :delete, class: "button", data: { confirm: 'Are you sure?' }, class: 'btn btn-warning'%>
			<% end %>
  	</p>
